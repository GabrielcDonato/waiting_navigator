import 'package:flutter/material.dart';

class Details3Page extends StatelessWidget {
  const Details3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Details 3"),
      ),
      body: Center(
        child: TextButton(
          onPressed: () {},
          child: Text("Back"),
        ),
      ),
    );
  }
}
